import express from "express";

const router = express.Router();

router.get("/", () => {});
router.post("/", () => {});

router.patch("/:id", () => {});
router.delete("/:id", () => {});

module.exports = router;
